// ISEQ5: Calculates the nth fibonacci number
//tlc type: run
//tlc case: 6=54,24
function signed gcd(signed a, signed b) {
  if(a == 0)
    b;
  else {
    gcd(b, (a - ((a / b) * b)));
  }
}
signed x, y;
gcd(x, y);
